{
  "name": "docker-management-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "dockerode": "^4.0.7",
    "express": "^5.1.0",
    "openid-client": "^6.8.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
